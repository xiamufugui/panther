fragment PolicySummary on Policy {
  id
  description
  displayName
  resourceTypes
  complianceStatus
  outputIds
  runbook
  reference
  severity
  tags
  createdAt
  lastModified
  enabled
  analysisType
}
