fragment RuleSummary on Rule {
  id
  description
  displayName
  logTypes
  threshold
  outputIds
  runbook
  reference
  severity
  tags
  dedupPeriodMinutes
  createdAt
  lastModified
  enabled
  analysisType
}
