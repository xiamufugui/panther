query ListRules($input: ListRulesInput) {
  rules(input: $input) {
    rules {
      lastModified
      logTypes
      severity
      id
      displayName
      threshold
      enabled
      outputIds
    }
    paging {
      totalPages
      thisPage
      totalItems
    }
  }
}
